# E2E 测试额外依赖 (在项目根 requirements.txt 之外)
#
# 安装:
#   python3 -m pip install --user --break-system-packages -r tests/e2e/requirements-dev.txt

playwright>=1.40.0
pytest-playwright>=0.5.0
pytest-html>=4.1.0
