# Agent Dev Playground — 依赖清单
# 安装：pip3 install --user -r requirements.txt

anthropic>=0.39.0
openai>=1.30.0
pydantic>=2.0.0
streamlit>=1.38.0
python-dotenv>=1.0.0

# LangGraph 重构版依赖
langgraph>=0.2.0
langchain-core>=0.3.0

# 工具与配置
pyyaml>=6.0          # memory_store 用 (E2E 测试发现)
opentelemetry-api>=1.20.0   # 链路追踪 (E2E 测试发现)
opentelemetry-sdk>=1.20.0
