# 复制此文件为 .env 并填入你的 API Key
# cp .env.example .env

# Anthropic Claude API Key
# 获取地址：https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-your_key_here

# OpenAI GPT API Key
# 获取地址：https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your_key_here

# DeepSeek API Key（可选，Day 2 扩展用）
# DEEPSEEK_API_KEY=sk-your_deepseek_key

# 默认 LLM 配置
DEFAULT_PROVIDER=anthropic
DEFAULT_MODEL=claude-sonnet-4-20250514
DEFAULT_TEMPERATURE=0.7
DEFAULT_MAX_TOKENS=4096

# [DEBUG] 上下文窗口覆盖（仅供压缩流程快速验证用）
# 留空 = 使用模型默认配置（GLM=128K, Claude=200K）
# 设置后：按固定缓冲或比例阈值触发压缩
# 调试推荐：CONTEXT_WINDOW_OVERRIDE=20000
# CONTEXT_WINDOW_OVERRIDE=20000

# [DEBUG] 压缩触发比例覆盖（对齐 Claude Code CLAUDE_AUTOCOMPACT_PCT_OVERRIDE）
# 留空 = 使用固定缓冲模式（GLM 13K buffer, Claude 13K buffer）
# 设为 10 = 总预算的 10% 时触发压缩（测试用，取 min(比例, 固定) 更保守的值）
# AUTOCOMPACT_PCT_OVERRIDE=10
